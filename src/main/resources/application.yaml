spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/veiculosdb
    username: postgres
    password: teamo1
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
  properties:
    hibernate:
      dialect: org.hibernate.dialect.PostgreSQLDialect
      format_sql: true